<div class="tbc <%= (defined? style) ? style : "" %>"><div class="container"><div class="tb">
  <div class="w100"><a href="<%= root_path %>"><%= image_tag APP_CONFIG['cdn_base_url'] + "/images/logo-m.png" %></a></div>
  <div>
    <div class="navbar navbar-inverse">
      <ul class="nav pull-right">
        <li><%= link_to t("label.browse"), browse_path, :title => t("label.browse"), :class => "tipno" %></a></li>
        <% if !user_signed_in? %>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= t("label.log_in") %></a>
          <ul class="dropdown-menu tli">
            <li>
              <div class="tlif"><div class="tli">
<%= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
<div class="disbl"><%= f.text_field :email, :placeholder => t("label.email") %></div>
<div class="disbl"><%= f.password_field :password, :placeholder => t("label.password") %></div>
<div class="disbl"><label class="checkbox"><%= f.check_box :remember_me %><%= t("label.remember_me")%></label></div>
<div class="disbl"><%= f.submit t("label.log_in"), :class => "btn btn-info w100" %></div>
<% end %>
<div class="disbl mt10 tac"><a href="<%= new_user_password_path %>"><%= t("label.forgot_password")%></a></div>
              </div><div class="tsu">
                <span class="disbl"><%= t("txt.no_acc") %></span>
                <span class="disbl"><%= link_to t("label.signup_here"), new_user_registration_path %></a></span>
              </div></div>
            </li>
          </ul>
        </li>
        <% end %>
      </ul>
    </div>
  </div>
  <% if user_signed_in? %>
  <div>
    <div class="navbar navbar-inverse">
      <ul class="nav pull-right">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown"><i class="icon-white icon-user"></i></a>
          <ul class="dropdown-menu tmn">
            <li><a href="<%= signout_path %>"><%= t("label.sign_out") %></a></li>
          </ul>
        </li>
      </ul>
    </div>
  </div>
  <% end %>
  <% if !user_signed_in? %>
  <div><a href="<%= new_registration_path(resource_name) %>" class="btn btn-info"><%= t("label.get_prod")%></a></div>
  <% end %>
</div></div></div>
